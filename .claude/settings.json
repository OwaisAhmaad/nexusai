{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "file=$(echo '$CLAUDE_TOOL_INPUT' | python3 -c \"import sys,json; d=json.load(sys.stdin); print(d.get('file_path',''))\" 2>/dev/null); if echo \"$file\" | grep -q 'apps/api/src.*\\.ts$'; then cd /home/user/nexusai/apps/api && npx tsc --noEmit --skipLibCheck 2>&1 | head -20 || true; fi"
          }
        ]
      }
    ]
  },
  "env": {
    "NEXT_PUBLIC_API_URL": "http://localhost:3001"
  }
}
